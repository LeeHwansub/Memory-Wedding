spring:
  application:
    name: memory-wedding-api
  profiles:
    active: dev

server:
  port: 8080

app:
  cors:
    allowed-origins: http://localhost:3000
  frontend-url: http://localhost:3000
  drive:
    redirect-uri: ${DRIVE_REDIRECT_URI:http://localhost:8080/api/drive/oauth/callback}
  gemini:
    api-key: ${GEMINI_API_KEY:}
    model: ${GEMINI_MODEL:gemini-2.5-flash}
    max-photos: ${GEMINI_MAX_PHOTOS:20}
    min-confidence: ${GEMINI_MIN_CONFIDENCE:0.60}
    max-videos: ${GEMINI_MAX_VIDEOS:5}
    video-frames: ${GEMINI_VIDEO_FRAMES:5}
  jwt:
    secret: change-me-in-production-use-long-random-string-at-least-32-chars
    expiration-ms: 86400000
